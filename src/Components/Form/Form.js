import React from 'react';
import './Form.css';
import Input from '../Input/Input';

class Form extends React.Component {
    render() {
        return (
            <form>
                < Input />
                < Input />
            </form>
        )
    }
}

export default Form;